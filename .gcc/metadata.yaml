project: nid-industry-interface
phase: milestone-2-in-progress
status: jd-posting-slice-complete
test_count: 0
created: 2026-05-29
last_updated: 2026-05-29

# This project is fully isolated. It does NOT register with any
# global GCC index (e.g. ~/CLAUDE CONTEXT/GCC/main.md). Its state
# lives entirely within this folder.
isolated: true

stack:
  frontend: nextjs-15-app-router
  language_web: typescript-strict
  language_ml: python-3.13
  package_manager: pnpm-10.5.0
  database: postgres-16
  orm: drizzle
  ui: tailwind-v4 + shadcn-ui
  host_prototype: vercel-fluid-compute
  host_production: self-hosted-docker-on-stqc-cleared-cloud
  observability: langfuse-self-hosted

milestones_completed:
  - foundations  # 2026-05-29

milestones_in_progress:
  - recruiter-end-to-end-mock
  # done: onboarding (/apply + /track) · admin recruiter queue · JD posting wizard
  # next: admin JD moderation (/admin/jds), then candidate browse

next_milestone: still-recruiter-end-to-end-mock

modules:
  - foundations          # complete
  - recruiter-onboarding # /apply + /track + admin queue; full onboarding loop runs end-to-end
  - jd-posting           # structured wizard + deterministic stipend-floor gate (no AI analyzer yet)
  # Admin UI for the recruiter queue lives in apps/web/app/admin (consumes the
  # recruiter-onboarding module's public API; no separate module yet).
  # Upcoming:
  # - admin-jd-moderation    (next — Phase 5.1; /admin/jds publishes in-moderation JDs)
  # - candidate-browse       (Phase 4.4)
  # - slot-booking           (Phase 4.6)
  # - interview-console-mobile (Phase 4.7)
  # - offer-cascade          (Phase 4.8)
  # - ai-jd-analyzer (Python ML worker) — supplies the real scope-creep multiplier
