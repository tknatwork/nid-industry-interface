# REFERENCES.md — interview-console pointers

## Plan sections
- Phase 4.7 (interview-day mobile console — transport selector + sandboxed demo + conflict awareness)
- Phase 4.17 (cross-interview conflict awareness, anonymized)
- Phase 6.5 (mobile-first vs desktop-first surfaces)

## Sibling modules
- `slot-booking` — supplies the real assignments that build the queue
- `candidate-browse` — supplies candidate names/disciplines
- `offer-cascade` (next) — the recruiter moves from interviews to offers

## File map
- `src/index.ts` / `src/types.ts` / `src/demo.ts` / `src/store.ts` (`.dev-data/interview-console.json`) / `src/actions.ts`

NOTE: mock data at `apps/web/.dev-data/` under `pnpm --filter web dev`.
