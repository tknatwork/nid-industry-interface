{
  "name": "@nid/module-student-portal",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Student portal (light) — cycle opt-in, eligible-JD feed, application tracker, real offer inbox (Phase 3.5). Consumes published outputs of the recruiter-side modules.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint placeholder'",
    "test": "echo 'no tests yet'"
  },
  "dependencies": {
    "@nid/core": "workspace:*",
    "@nid/db": "workspace:*",
    "@nid/module-candidate-browse": "workspace:*",
    "@nid/module-jd-posting": "workspace:*",
    "zod": "3.23.8"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
